get:
  summary: Get super fun time
  x-security:
    hmac_auth:
        - 'api:read:warehouse-policies'
  operationId: getFunTimeById
  tags:
    - Fun Times
  parameters:
    - $ref: '../parameters/PathId.yaml'
  responses:
    200:
      schema:
        $ref: '../definitions/SuperFunTime.yaml'
      description: I want a super fun time
